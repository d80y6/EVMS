apiVersion: v1
kind: Secret
metadata:
  name: vms-secrets
  namespace: dam-vms
type: Opaque
  # WARNING: Replace these with secure values before deploying to production
stringData:
  db-url: "postgres://dam_admin:chang3m3!@postgres:5432/dam_vms?sslmode=disable"
  jwt-secret: "REPLACE-WITH-SECURE-RANDOM-SECRET-64-CHARS-MIN"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recordings-pvc
  namespace: dam-vms
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
